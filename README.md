# Luma Health

Healthcare web UI in Next.js and TypeScript. In progress — frontend only, no database yet.

## Stack
- Next.js / TypeScript / React
- Tailwind CSS and shadcn/ui

## Run
```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
